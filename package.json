{
  "name": "playwright-mcp-cursor",
  "version": "1.0.0",
  "description": "Playwright project demonstrating cursor-based pagination using an e-commerce site and integration with the Model Context Protocol (MCP).",
  "scripts": {
    "test": "npx playwright test",
    "install:playwright": "npx playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.1",
    "typescript": "^5.2.2"
  }
}
